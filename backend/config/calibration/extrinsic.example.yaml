# Extrinsic calibration result: camera-to-flange offset.
#
# Generated by: python -m tools.calibrate.extrinsic
#
# The offset (dx, dy) represents the camera optical center position
# relative to the robot flange center, in mm.

calibration_date: "2026-03-17T10:30:00"
mm_per_pixel: 0.009857

T_cam_to_flange:
  dx: -2.5    # mm, camera optical center X offset from flange
  dy: 1.3     # mm, camera optical center Y offset from flange

flip_y: false  # set true if world Y axis is opposite to image Y axis

# Raw data used during calibration (for traceability)
calibration_data:
  pixel: [740, 412]
  arm_pose: [100.0, 200.0]
  principal_point: [1023.2, 766.3]
