workflow:
  name: "segmentation_workflow"
  initial: "idle"
  states:
    - idle
    - running
  transitions:
    - trigger: "SYS:STARTED"
      source: "idle"
      dest: "running"
  tasks:
    running: "seg_pick"
  side_tasks:
    - "pixel_to_world"
    - "plc_orchestrator"
