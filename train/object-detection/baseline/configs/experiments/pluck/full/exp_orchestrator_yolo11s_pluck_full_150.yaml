experiment:
  name: yolo11s_full_pluck
  framework: yolo
  output_dir: experiments/pluck/full/yolo11s
shared_resources:
  datasets_root: .
  weights_cache: models
framework_cfg:
  project_dir: frameworks/yolo
  script: frameworks/yolo/scripts/train.py
  args:
    model: yolo11s.pt
    data_yaml: dataset/pluck_subset150_tiles640.yaml
    epochs: 100
    batch: 32
    imgsz: 640
    device: 0
    workers: 4
    task_type: hbb
    nbs: 64
