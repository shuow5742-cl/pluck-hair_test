experiment:
  name: rtdetr_full_pluck
  framework: yolo
  output_dir: experiments/pluck/full/rtdetr
shared_resources:
  datasets_root: .
  weights_cache: models
framework_cfg:
  project_dir: frameworks/yolo
  script: frameworks/yolo/scripts/train.py
  args:
    model: rtdetr-l.pt
    data_yaml: dataset/pluck_subset150_tiles640.yaml
    epochs: 100
    batch: 8
    imgsz: 640
    device: 0
    workers: 4
    task_type: hbb
    nbs: 64
