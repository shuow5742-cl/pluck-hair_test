# YOLO Framework Subproject

This directory contains the isolated Ultralytics YOLO runner that the orchestrator dispatches.
Use `uv run --project frameworks/yolo python scripts/train.py --help` for usage details.
